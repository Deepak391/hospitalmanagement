import 'package:flutter/material.dart';

class Medshop extends StatelessWidget {
  const Medshop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Med shop"),),
    );
  }
}
